git使用方法，环境搭建
cd ..返回上一层  ls 运行
创建目录 mkdir 创建一个新的空文件 touch  复制 cp 移动 mv  删除目录 rmdir（为空） -r(不为空)递归会永远消失 删除文件 rm
下载在特定的 url 找到的文件：curl  在较大的文件体中寻找特定的片段：grep  主页查看文件的内容：less, cat
操作和转换文本流（例如，讲 HTML 文件中<div>的所有实例改为<article>）: awk, tr, sed
